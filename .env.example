# GrowthOS runs entirely with deterministic local providers by default.
AI_PROVIDER=mock

# Optional server-only extension points for a real provider implementation.
AI_PROVIDER_API_KEY=
AI_PROVIDER_MODEL=
AI_PROVIDER_BASE_URL=
