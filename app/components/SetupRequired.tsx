import { Database, KeyRound, ServerCog } from "lucide-react";

export function SetupRequired() {
  return <main className="onboarding-page"><div className="onboarding-logo wordmark"><span>G</span><b>GrowthOS</b></div><section className="onboarding-card"><div className="icon-tile"><ServerCog size={24} /></div><p className="kicker">Production setup required</p><h1>Connect the real application foundation</h1><p className="muted">This build intentionally has no seeded workspace, demo login, fake provider, or canned AI fallback. Configure a Supabase environment to create the first real account.</p><div className="notice info"><Database size={18} /><div><strong>1. Apply the Supabase migration</strong><p>Run the SQL migration in <code>supabase/migrations</code> against a development Supabase project.</p></div></div><div className="notice info"><KeyRound size={18} /><div><strong>2. Add environment variables</strong><p>Set <code>NEXT_PUBLIC_SUPABASE_URL</code>, <code>NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY</code>, and <code>SUPABASE_SECRET_KEY</code>.</p></div></div><p className="fine-print">GrowthOS remains unavailable until real infrastructure is configured. That is intentional.</p></section></main>;
}
